#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, e;
    for (int i = 0; i < 2; i++) {
        cin >> a >> b >> c >> d >> e;
        cout << 6 * a + 3 * b + 2 * c + d + 2 * e << " ";
    }
    cout << "\n";
    
    return 0;
}