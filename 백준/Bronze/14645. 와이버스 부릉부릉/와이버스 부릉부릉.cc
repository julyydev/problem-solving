#include <iostream>
#define FASTIO() ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL)
using namespace std;

int main() {
    FASTIO();
    int N, K, A, B;
    cin >> N >> K;
    while (N--) cin >> A >> B;
    cout << "비와이\n";

    return 0;
}